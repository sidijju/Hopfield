program: main.py
project: language_model_benchmarks
method: grid

parameters:
  model:
    parameters:
      name:
        values: ["rnn", "linear_attention", "hopfield_attention"]

  training:
    parameters:
      sequence_length:
        values : [1024, 2048, 4096, 8192]

  benchmark:
    parameters:
      name:
        # ["lambada", "wikitext", "memcopy", "lra"]
        values: ["lambada"]
